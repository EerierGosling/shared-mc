# engines in package.json is advisory by default: npm prints EBADENGINE and
# installs anyway. minecraft-protocol needs node >=22 and fails in subtle ways
# below that, so make the wrong runtime a hard error at install time instead.
engine-strict=true
